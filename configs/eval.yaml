# VAE Evaluation Configuration

_target_: proteindiff.eval.eval_vae.EvalVAECfg

# Checkpoint path (required - set via CLI)
checkpoint_path: "/home/ubuntu/proteinDiffVirgina/experiments/run/2026-01-19/07-43-05/checkpoint_step-4,000.pt"

# Output directory for generated structures
output_dir: "../eval_output"

# Number of latent samples to generate per input
num_samples: 10

# Data configuration
data:
  _target_: proteindiff.training.data.data_loader.DataHolderCfg

  data_path: /home/ubuntu/proteinDiffVirgina/data/pdb_2021aug02

  # Use test split only
  num_train: 0
  num_val: 0
  num_test: 64

  batch_tokens: 4096

  min_seq_size: 16
  max_seq_size: 256
  max_resolution: 3.5
  homo_thresh: 0.70
  asymmetric_units_only: false

  num_workers: 1
  prefetch_factor: 1
  rng_seed: 42
  buffer_size: 32
